# Go library project Makefile

Common Makefile for Go library projects.


## Installation

Download the file:

```shell
curl https://raw.githubusercontent.com/sagikazarmark/makefiles/master/go-library/main.mk > main.mk
```

Add it to the project Makefile:

```Makefile
include main.mk
```
