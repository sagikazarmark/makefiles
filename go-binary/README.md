# Go binary project Makefile

Common Makefile for single-binary Go projects.


## Installation

Download the file:

```shell
curl https://raw.githubusercontent.com/sagikazarmark/makefiles/master/go-binary/main.mk > main.mk
```

Add it to the project Makefile:

```Makefile
include main.mk
```
